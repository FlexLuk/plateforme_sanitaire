# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    #- name: Setup .NET
     # uses: actions/setup-dotnet@v3
      #with:
       # dotnet-version: 6.0.x
    #- name: Restore dependencies
    #  run: dotnet restore
    #- name: Build
    #  run: dotnet build --no-restore
    #- name: Test
     # run: dotnet test --no-build --verbosity normal
    #- name: Publish
     # shell: pwsh
      #run: dotnet publish -c Release -o .\myApp
    - name: Connect on WinRM
      shell: pwsh
      run: |
        winrm set winrm/config/client ‘@{TrustedHosts="*"}’
        $password = ${{ secrets.LOGIN_PWD }}
        [SecureString]$securepassword = $password | ConvertTo-SecureString -AsPlainText -Force 
        $credential = New-Object System.Management.Automation.PSCredential -ArgumentList ${{ secrets.LOGIN_USER }}, $securepassword
        pwd
        Invoke-Command -ComputerName plateforme.osiet.net -ScriptBlock { iisreset /stop } -credential $credential
        Invoke-Command -ComputerName plateforme.osiet.net -ScriptBlock { iisreset /start } -credential $credential
        Invoke-Command -ComputerName plateforme.osiet.net -ScriptBlock { exit } -credential $credential
        exit
